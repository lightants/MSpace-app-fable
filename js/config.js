// Address of the MSpace booking server (Node app in this repo). Leave empty when the pages are
// served by the Node server itself. When hosted on GitHub Pages, set it to the backend URL, e.g.
//   window.MSPACE_API = 'https://mspace-booking.onrender.com';
window.MSPACE_API = '';
